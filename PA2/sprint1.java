public static class Game{
this.pebbles = 4;
this.isBasket = false;
this.currPlayerSide = true;
this.twoPlayer = false;
}

if(isValidMove()){}

public static bool isValidMove(userInput){
   if( userInput == currPlayerSide && (Game[userInput].pebbles) != 0 && !(isBasket) )
	return true;
   else
	return false;
}


if( isWinner() ){}

public static bool isPlayerWinner(){
	if( player.basket > computer.basket ){
		return true;
	}
	else{
		return false;
	}
}

if( anotherTurn() ){}

public static bool anotherTurn(){
	if( (Game[userInput].pebblesLeft == 0 && Game[Game[userInput].pebbles + userInput].isBasket){
		return true;
	else
		return false;
}

public static void main(){
	Game board[] = new Game[12];


}
